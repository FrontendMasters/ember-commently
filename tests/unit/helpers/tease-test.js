
import { tease } from 'commently/helpers/tease';
import { module, test } from 'qunit';

module('Unit | Helper | tease');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tease([42]);
  assert.ok(result);
});

